package com.codewithchang;

public class Main {

    public static void main(String[] args) {
        // a mile is equal to 1.609344 kilometers
        double kilometers = (100 * 1.609344); // an expression is everything from kilometers excluding the semi-colon
        int highScore = 50; // highScore = 50 is the expression

        if (highScore == 50) { // everything inside of the parentheses is an expression
            System.out.println("This is an expression");
        }
    }
}
